overwrite: true
schema: ${SCHEMA_PATH}
documents: '**/*.graphql'
generates:
  ./front/graphql/__generated__/graphql.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
